<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/FLInGameEditorRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Top floating toolbar matching controls_editor_activity style -->
    <LinearLayout
        android:id="@+id/LLInGameEditorToolbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="8dp"
        android:background="@drawable/input_controls_toolbar"
        android:elevation="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="4dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingLeft="8dp"
            android:paddingRight="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                android:textSize="12sp"
                android:textColor="#ffffff"
                android:text="@string/profile" />

            <TextView
                android:id="@+id/TVProfileName"
                android:layout_width="wrap_content"
                android:textColor="@color/colorAccent"
                android:textSize="13sp"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <View style="@style/VerticalLine" />

        <ImageButton
            android:id="@+id/BTAddElement"
            style="@style/ToolButton"
            android:contentDescription="Add Element"
            android:src="@drawable/icon_add" />

        <ImageButton
            android:id="@+id/BTRemoveElement"
            style="@style/ToolButton"
            android:contentDescription="Remove Element"
            android:src="@drawable/icon_remove" />

        <ImageButton
            android:id="@+id/BTElementSettings"
            style="@style/ToolButton"
            android:contentDescription="Element Settings"
            android:src="@drawable/icon_settings" />

        <ImageButton
            android:id="@+id/BTRadialWheel"
            style="@style/ToolButton"
            android:contentDescription="Radial Wheel"
            android:src="@drawable/icon_radial_wheel" />

        <View style="@style/VerticalLine" />

        <ImageButton
            android:id="@+id/BTStylePreset"
            style="@style/ToolButton"
            android:contentDescription="Style Preset"
            android:src="@drawable/icon_palette" />

        <ImageButton
            android:id="@+id/BTOpacity"
            style="@style/ToolButton"
            android:contentDescription="Global Opacity"
            android:src="@drawable/icon_opacity" />

        <View style="@style/VerticalLine" />

        <ImageButton
            android:id="@+id/BTReset"
            style="@style/ToolButton"
            android:contentDescription="Reset Layout"
            android:src="@drawable/icon_reset" />

        <ImageButton
            android:id="@+id/BTDone"
            style="@style/ToolButton"
            android:contentDescription="Done"
            android:src="@drawable/icon_confirm" />

    </LinearLayout>

</FrameLayout>
